{% extends 'base.html.twig' %}

{% block title %}{{ category.name }}{% endblock %}

{% block body %}
    <h1>{{ category.name }}</h1>
    <a class"btn btn-primary" href="{{ path('app_categories_edit', { id: category.id }) }}">Modifier</a>
{% endblock %}
